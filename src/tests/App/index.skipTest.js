// TODO Vlad config jest to work with components 

// import App from '.'
// import React from 'react'
// import {
// 	getURLParams as mockGetUrlParams,
// 	getCellWidthHeight as mockGetCellWidthHeight
// } from '../../components/App/app.utils'

// const mockUrlParams = {radius: 'radius'}

// jest.mock('../../src/components/App/app.utils', () => ({
// 	etURLParams: jest.fn(() => mockUrlParams),
// 	getCellWidthHeight: jest.fn()
// }))

// jest.mock('../../src/components/App/app.utils', () => ({
// 	etURLParams: jest.fn(() => mockUrlParams),
// 	getCellWidthHeight: jest.fn()
// }))

// const renderComponent = () => <App/>

// describe('App component', () => {
// 	it('should render component and call getURLParams and getCellWidthHeight', () => {
// 		renderComponent()

// 		expect(mockGetUrlParams).toBeCalledTimes(1)
// 		expect(mockGetCellWidthHeight).toBeCalledTimes(1)

// 		expect(mockGetCellWidthHeight).toBeCalledWith(mockUrlParams)
    
// 	})

// })